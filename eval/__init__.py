"""Phase 4 evaluation harness (ROADMAP.md Phase 4). Not part of the dale package's
public API — this is this project's own research-validation tooling, the same
role examples/ plays for demos. Requires the `agent` extra (`uv sync --extra
agent`)."""
